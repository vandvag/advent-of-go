package aoccli

import (
	"context"

	"github.com/urfave/cli/v3"
)

const (
	cmdRunLabel = "run"
)

func commands(ctx context.Context, cmd *cli.Command) []*cli.Command {
	cmds := []*cli.Command{
	}

	return cmds
}
